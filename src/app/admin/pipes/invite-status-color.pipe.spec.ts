import { InviteStatusColorPipe } from './invite-status-color.pipe';

describe('InviteStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new InviteStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
